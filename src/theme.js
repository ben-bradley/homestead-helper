import { createTheme } from "@mui/material";

const theme = {
  palette: {
    primary: {
      main: "#56300F"
    },
    secondary: {
      main: "#70A437"
    }
  }
};

export default createTheme(theme);
